spring.application.name=panaderia-backend-test

# Base de datos para pruebas
spring.datasource.url=jdbc:postgresql://localhost:5432/db_panaderia_test
spring.datasource.username=postgres
spring.datasource.password=gian123
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true
# Opcional: muestra las consultas SQL en la consola

# Contexto de pruebas
server.servlet.context-path=/api/test
